package com.proximusinspired.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS is handled by the API Gateway, not by individual services
}
